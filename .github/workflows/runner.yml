name: runner_ipk

on:
  push:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: cdmake
      run: cd ../../
    - name: make
      run: make
    - name: cdtest
      run: cd ../../
    - name: test
      run: ./test_ipk.sh > tests.out
      
      
