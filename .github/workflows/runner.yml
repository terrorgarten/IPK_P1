name: runner_ipk

on:
  push:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: make
      run: make
    - name: test
      run: ./test_ipk.sh > tests.out
      
      
